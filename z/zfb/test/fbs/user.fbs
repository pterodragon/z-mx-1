namespace zfbtest;
table UserID {
  id:uint64;
}
table User {
  id:uint64 (key);
  name:string;
  passwd:[ubyte];	// output of HMAC-SHA256 with password key
  passwdKey:[ubyte];	// hash secret (random at time of passwd setting)
  roles:[string];	// roles
}
table UserList {
  list:[User];
}
