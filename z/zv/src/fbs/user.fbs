namespace ZvUserDB.fbs;
table UserID {
  id:uint64;
}
table User {
  id:uint64 (key);
  name:string;
  passwd:[ubyte];	// HMAC-SHA256 of secret, password
  secret:[ubyte];	// secret (random at user creation)
  roles:[string];	// roles
}
table UserList {
  list:[User];
}
