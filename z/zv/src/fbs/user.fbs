namespace ZvUserDB.fbs;
table UserID {
  id:uint64;
}
table User {
  id:uint64 (key);
  name:string;
  hmac:[ubyte];		// HMAC-SHA256 of secret, password
  secret:[ubyte];	// secret (random at user creation)
  roles:[string];	// interactive roles
  flags:uint8;
}
table UserList {
  list:[User];
}
