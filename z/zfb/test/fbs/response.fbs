include "perm.fbs";
include "role.fbs";
include "user.fbs";
include "key.fbs";
namespace zfbtest;
union RespData {
  UserGet:UserList,
  UserAdd:User,
  UserMod:User,
  UserDel:UserID,
  
  RoleGet:RoleList,
  RoleAdd:Role,
  RoleMod:Role,
  RoleDel:RoleID,
  
  PermGet:PermList,
  PermAdd:Perm,
  PermMod:Perm,
  PermDel:PermID,

  KeyGet:KeyList,
  KeyAdd:Key,
  KeyClr:KeyIDList,
  KeyDel:KeyID
}
table Response {
  id:uint64;		// request ID
  data:RespData;
}
root_type Response;
