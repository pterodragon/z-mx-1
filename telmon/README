
To build:

0. in shell
1. "ln -s PATH/TO/BUILD/DIRECTORY build_directory"
2. "export LD_LIBRARY_PATH=`pwd`/build_directory/lib"
3. "qmake TelMon.pro"
4. "make"

To run:
1. "./TelMon" 


Notes:
-- build tested on:
QMake version 3.1
Using Qt version 5.9.5 in /usr/lib/x86_64-linux-gnu

